# nodejs example

Run from this directory with the runtime for nodejs. Server examples fail fast when `WALOOKUP_API_KEY` is absent. Browser JavaScript calls a same-origin proxy and never contains an API key.
